import React from "react";

export default function ExampleOfProps(props) {
      return (
            <div>
                  <h1>Hello {props.name} Age is {props.age}</h1>
            </div>
      )
}