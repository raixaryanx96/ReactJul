//Contact.jsx

import { Component } from "react";

export default class Contactclass extends Component {
      render() {
            return (
                  <div>
                        <h1>This contact saved : {this.props.name}</h1>
                  </div>
            )
      }
}